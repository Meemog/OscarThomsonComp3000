upstream frontend {
    server frontend:8000;
}

upstream backend {
    server backend:3000;
}

server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    location / {
        proxy_pass http://frontend/;
    }

    location /api/ {
        proxy_pass http://backend/;
    }

}